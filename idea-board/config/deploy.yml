service: idea-board
image: godev-vibe/idea-board

servers:
  web:
    - 89.19.208.212

proxy:
  ssl: true
  host: ideas.olezhek28.courses
  app_port: 3100
  healthcheck:
    path: /api/auth/me

registry:
  server: ghcr.io
  username: godev-vibe
  password:
    - KAMAL_REGISTRY_PASSWORD

volumes:
  - "idea-board-data:/app/data"

env:
  secret:
    - TELEGRAM_BOT_TOKEN
    - TELEGRAM_BOT_USERNAME
    - JWT_SECRET
    - ADMIN_TELEGRAM_ID
    - WEBHOOK_SECRET
  clear:
    NEXT_PUBLIC_APP_URL: https://ideas.olezhek28.courses
    DATABASE_PATH: /app/data/ideas.db
    MAX_VOTES: "5"
    REGEN_INTERVAL_HOURS: "24"
    MONTHLY_IDEA_LIMIT: "10"

builder:
  arch: amd64
